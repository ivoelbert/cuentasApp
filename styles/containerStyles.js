import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    icon: {
        width: 24,
        height: 24
    },
    container: {
        flex: 92,
        backgroundColor: '#fff',
        alignItems: 'center'
    }
});

export default styles;